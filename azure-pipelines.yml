# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- none

pool:
  vmImage: ubuntu-latest

steps:
- task: TerraformTaskV4@4
  inputs:
    provider: 'azurerm'
    command: 'init'
    backendServiceArm: 'conn-sub-daz-sandbox'
    backendAzureRmResourceGroupName: 'terraform-state'
    backendAzureRmStorageAccountName: 'sacstatefile0001'
    backendAzureRmContainerName: 'tf-state-virtualnetwork'
    backendAzureRmKey: 'devops3.terraform.tfstate'
    backendAWSBucketName: 
- task: TerraformTaskV4@4
  inputs:
    provider: 'azurerm'
    command: 'apply'
    environmentServiceNameAzureRM: 'conn-sub-daz-sandbox'
